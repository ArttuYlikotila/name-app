{
  "name": "name-application",
  "version": "1.0.0",
  "engines": {
    "node" : "14.x"
  },
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Arttu Ylikotila",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
